[Cursor] Fix Missing Voice Assistant Button - Add Error Handling

- Fix missing "Talk Now" voice assistant button by adding safe VAPI import
- Add error handling for VAPI Web SDK import failures
- Add fallback UI when VAPI is not available
- Change VAPI import from ES6 import to require() with try-catch
- Add proper error boundaries to prevent component crashes
- Create VoiceAssistantSimple component for testing

Fixes:
- Missing voice assistant button due to VAPI import errors
- Component crashes preventing button from rendering
- Better error handling for VAPI Web SDK issues
- Graceful fallback when VAPI is not available